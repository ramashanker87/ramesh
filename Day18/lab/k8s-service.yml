apiVersion: v1
kind: Service
metadata:
  name: ramesh-ecr-service
  namespace: rameshday18
spec:
  type: LoadBalancer
  selector:
    app: ramesh-ecr-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
